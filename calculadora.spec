# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['calculadora.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='calculadora',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['/Users/dalton/Downloads/Calculadora do iPhone/icone.png'],
)
app = BUNDLE(
    exe,
    name='calculadora.app',
    icon='/Users/dalton/Downloads/Calculadora do iPhone/icone.png',
    bundle_identifier=None,
)
